project (dbportal_installs)

if (EXISTS ${CMAKE_BINARY_DIR}/pocioci/pocioci${EXEC_EXT})
  install_exec (${CMAKE_BINARY_DIR}/pocioci/pocioci${EXEC_EXT} ${TOOLS_DIR})
endif ()  
if (EXISTS ${CMAKE_BINARY_DIR}/pocilink/pocilink${EXEC_EXT})
  install_exec (${CMAKE_BINARY_DIR}/pocilink/pocilink${EXEC_EXT} ${TOOLS_DIR})
endif ()  
if (EXISTS ${CMAKE_BINARY_DIR}/pocidump/pocidump${EXEC_EXT})
  install_exec (${CMAKE_BINARY_DIR}/pocidump/pocidump${EXEC_EXT} ${TOOLS_DIR})
endif ()  
